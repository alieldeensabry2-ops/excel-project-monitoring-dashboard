# VBA Code

This folder contains the exported VBA components used in the Excel dashboard project.

The VBA code is responsible for:
- search interaction
- dashboard filtering
- UI control behavior
- UserForm logic
- chart handling
- performance optimization

The code files will be added in exported form from the original Excel workbook, including modules, class modules, and forms.
